# MGL Skin

## 安装与运行

使用 Node.js 22.13 或以上版本运行服务，通过 Bun 安装依赖：

```bash
bun install
```

开发环境：

```bash
bun run dev
```

默认开发地址为 `http://localhost:4300`。

生产环境：

```bash
bun run build
node .output/server/index.mjs
```
